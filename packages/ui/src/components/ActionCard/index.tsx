// TODO: <ActionPanel/> (only implement when required in payments
import { FC } from 'react';
import styled from 'styled';

export const Container = styled.div`

`;

export const ActionCard: FC = () => {
  return (
    <Container>container</Container>
  );
};
